import * as React from 'react';

export const APP = () => {

    return (
        <div className="App" >
            <h1>Welcome to React with Typescript, TSlint and Webpack 4 config.</h1>
        </div>
    );
}
